//the numbers for array 
const start = 10;
const end = 30;
//How to tget even numbers
const evenNumbers = [];
for (let i = start; i <= end; i += 2) {
    evenNumbers.push(i);
}
console.log(evenNumbers);

